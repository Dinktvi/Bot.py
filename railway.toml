[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway"

[deploy]
healthcheckTimeoutSeconds = 600
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[deploy.volumes]
# Persist SQLite DB + generated scripts across deploys
"data" = "/app/data"
